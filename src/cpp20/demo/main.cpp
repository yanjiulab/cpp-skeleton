// C++20 demo: coroutines mention (requires asio and compiler support)
#include <iostream>
#include <coroutine>
int main() {
    std::cout << "cpp20 demo: coroutines and concepts available if toolchain supports them."<< std::endl;
    return 0;
}
